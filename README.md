
  # Startup Job Board Design

  This is a code bundle for Startup Job Board Design. The original project is available at https://www.figma.com/design/MPTMdYx4T6aIuwWlL5gPRb/Startup-Job-Board-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # job-Board-design
